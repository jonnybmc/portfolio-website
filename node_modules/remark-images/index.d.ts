/**
 * Plugin to add a simpler image syntax.
 *
 * @type {import('unified').Plugin<[Options?]|void[], Root>}
 */
export default function remarkImages({
  imageExtensions
}?: void | Options | undefined):
  | void
  | import('unified').Transformer<import('mdast').Root, import('mdast').Root>
/**
 * Extensions recognized as images by default
 */
export const defaultImageExtensions: string[]
export type Root = import('mdast').Root
export type Image = import('mdast').Image
export type Link = import('mdast').Link
/**
 * Configuration (optional).
 */
export type Options = {
  /**
   * File extensions (without dot) to treat as images.
   */
  imageExtensions?: string[] | undefined
}
